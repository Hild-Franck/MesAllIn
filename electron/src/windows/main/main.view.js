var remote = require('remote'),
    remoteIpc = remote.require('ipc');

angular
    .module('MainView', [])
    .controller('MainCtrl', ['$scope', function(scope) {
        var vm = this;
        
    }]);